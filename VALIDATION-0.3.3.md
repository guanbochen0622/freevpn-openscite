# OpenScite 0.3.3 圖片解釋修正

## 原因與改動

舊提示要求九類內容，容易產生冗長回答。桌面版也曾把圖片事實抽取階段的 JSON 串流直接寫入回答區。

- 預設精簡：圖的重點、與前後文的關係、可支持的結論；有重要不確定性才補提醒。
- 可選詳細後重新解釋；五語回覆維持所選語言。
- 結構化分析不傳送正文串流，圖片分析期間介面也不顯示未驗證串流。
- 最終輸出經格式檢查，再轉為可讀文字；不直接顯示原始 JSON 或程式碼區塊。
- 前後文包含目前頁及前後頁，另外搜尋全文中的精確圖號，保留來源頁碼。
- 區分 Figure 1、Figure 10 與 Table 1，減少配錯上下文。
- 防止圖片分析重複啟動及分析期間換篇。

## 驗證範圍

底層測試新增：圖號邊界、跨頁圖號提及、格式異常、程式碼區塊拒絕、結構化串流保密。

Mac 原生視窗測試新增：主動注入模擬內部 JSON 串流，確認不出現在正文；兩階段分析後只顯示可讀結果；精簡/詳細請求切換；失敗停止。

本機及已部署網頁測試新增：實際 PDF 裁切與兩階段請求、可讀解釋、正文不出現 JSON 欄位。另測已上線網站的真實 OpenAlex/Crossref 搜尋。

受控 AI 回應測試驗證程式流程，不代表已測試使用者帳號的真實模型品質。長度指示仍需模型遵守；數值與科學結論應核對原文。圖片分析不是 OCR 完整性或推論正確性的保證。
