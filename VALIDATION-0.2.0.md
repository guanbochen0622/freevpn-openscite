# OpenScite 0.2.0 檢查紀錄

本版增加五語 Language 設定：English、繁體中文、简体中文、日本語、한국어。介面設定會保存，並傳到後續 AI 請求。論文原文、使用者輸入、筆記與既有 AI 回答不會因切換語言被翻譯。

## 修正

- 搜尋失敗的重試按鈕改用事件綁定，符合桌面版安全政策。
- 修正圖片分析呼叫不存在的函式，導致無法啟動的問題。
- 圖片分析失敗或取消時停止，不再自動重送模型請求。
- 空白或不可讀的 AI 回應顯示錯誤，不再把回傳 JSON 當成論文分析。
- 引用否定句與雙重否定採保守判定；DOI 不再只比對前綴。
- 引用查詢及引用 PDF 擷取會檢查目標是否已切換，防止舊結果覆蓋新結果。

## 可重跑的測試範圍

| 範圍 | 驗證方式 |
|---|---|
| 搜尋、篩選、比較、論文庫 | Chromium 操作測試；使用受控來源回應 |
| 搜尋失敗、不同請求先後完成、年份、OA 條件 | 失敗情境與回歸測試 |
| PDF 全文搜尋、跳頁、縮放、書籤、標註保存、重開 | 16 頁合成 PDF 的 Chromium 操作測試 |
| 文件替換、損壞文件、個別頁索引失敗 | 獨立底層測試 |
| Mac 文字選取 | Apple 晶片及 Intel 原生 Electron 視窗；75%、125%、200% 縮放下檢查文字寬度與位置，選取指定數字 |
| 解釋、問答、翻譯、研究關聯、摘要 | 原生 Mac 視窗送出請求，檢查論文內容與問題是否傳到桌面橋接層；使用模擬模型回答 |
| 圖片解釋 | 實際裁切頁面，驗證兩階段請求、兩张圖片、結構化資料要求及失敗後停止 |
| 帳號與模型連線 | 真實官方 app-server 初始化及未登入帳號查詢；模擬模型列表、取消、連線中斷、模型不存在等情境 |
| 五語 | 270 筆介面文案檢查、語言切換與保存、研究內容保護、手機寬度、AI 語言參數 |
| 備份與還原 | 下載、格式檢查、合併還原、排除 API 金鑰 |
| 網站部署路徑 | 同一套瀏覽器測試在根路徑及 /freevpn-openscite 執行 |

發布流程要求瀏覽器測試和兩種原生 Mac 測試全部通過，才建立 DMG 下載版本。完整執行紀錄見 GitHub Actions 的 Build Mac desktop。

此外，使用者提供的 15 頁論文已在本機以同版本 PDF.js 完成所有頁面的文字擷取與繪製；Node 測試環境另補齊瀏覽器內建的相容方法。這份私人論文未加入 GitHub 或公開 CI。

## 驗證界線

未登入使用者的 ChatGPT 帳號，故未驗證其訂閱權限、額度或真實模型回答品質。模擬回答通過只能證明程式傳遞與顯示流程正常，不代表科學解釋正確。

尚未涵蓋所有 PDF 字型、直排/旋轉文字、多欄選取、掃描文件 OCR、超大型文件、Safari/Firefox 的完整操作，以及各出版商的跨網域限制。期刊 Q1–Q4 為推估；引用立場為待人工核對的線索，不是官方分級或經確認的研究結論。

Mac 安裝檔仍為預覽版，尚未經 Apple 開發者簽章與公證。
